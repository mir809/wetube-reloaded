extends ../../base

block content
    if errorMessage
        span=errorMessage
    form(method="post") 
        input( name="oldPass" placeholder="기존 비밀번호" type="password" required)
        input( name="newPass" placeholder="새로운 비밀번호" type="password" required)
        input( name="newPass2" placeholder="새로운 비밀번호 확인" type="password" required)
        input(type="submit" value="Change Password")
